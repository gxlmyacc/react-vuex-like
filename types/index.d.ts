import { inject, Provider } from 'mobx-react';
import Store from './store';
export * from './store-helpers';
export { Provider, inject, Store };
declare const _default: {
    Store: typeof Store;
};
export default _default;
